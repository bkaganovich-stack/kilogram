package mobileproxy;

/**
 * Stub for the gomobile-generated StreamDialer class.
 *
 * Replace by placing the real mobileproxy.aar (generated via
 * "gomobile bind -target=android github.com/Jigsaw-Code/outline-sdk/x/mobileproxy")
 * into TMessagesProj/libs/ and adding:
 *   implementation fileTree(dir: 'libs', include: ['mobileproxy.aar'])
 * to TMessagesProj/build.gradle. Then delete this stub file.
 */
public abstract class StreamDialer {
    // Intentionally empty — the real class is provided by the gomobile AAR.
}
